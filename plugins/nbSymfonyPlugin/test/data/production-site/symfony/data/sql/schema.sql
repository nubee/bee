CREATE TABLE user (id BIGINT AUTO_INCREMENT, email VARCHAR(255) NOT NULL UNIQUE, test VARCHAR(255) NOT NULL UNIQUE, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, PRIMARY KEY(id)) ENGINE = INNODB;
