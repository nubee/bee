#command configuration
<?php
  $siteDir = '/var/www/site-directory';
  $symfonyDir = $siteDir.'/symfony';
?>

symfony2:
  dir: <?php echo $symfonyDir."\n"; ?>
  bin: php <?php echo $symfonyDir; ?>/app/console
  stage-path: <?php echo $siteDir; ?>
  web-dir: <?php echo $siteDir; ?>/httpdocs
  exec-sync: true
  exec-migrate: true
  exec-cache-clear: true
  exec-change-owner: true
  exec-change-mode: true
  exec-assets-install: true

#sync command
filesystem:
  dir-transfer:
      source-path: .
      target-path: <?php echo $siteDir; ?>
      exclude-from: .bee/excludes
      include-from: .bee/includes
