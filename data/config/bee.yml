project:
  name: the project name
  description: a simple description
#  commands:
#    test:
#      unit: "lime:test"
#      all: []
#    default:
#      integrate: ["lime:test"]
  bee:  # bee extra configuration keys
    enabled_plugins: ~
  repository:
    type: git
    root-directory: /set/an/absolute/path
  test-command: the test command
  shell:
    aliases:
      myAlias: echo "you called myAlias"
      dir-lin: dir --help
      dir-win: dir /?
